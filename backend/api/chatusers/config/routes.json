{
  "routes": [
    {
      "method": "GET",
      "path": "/chatusers",
      "handler": "chatusers.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/chatusers/count",
      "handler": "chatusers.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/chatusers/:id",
      "handler": "chatusers.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/chatusers",
      "handler": "chatusers.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/chatusers/:id",
      "handler": "chatusers.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/chatusers/:id",
      "handler": "chatusers.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
